name: Build Kernel with KernelSU Next

on:
  workflow_dispatch:
  push:
    branches: [kernelsu-integration]

jobs:
  build-kernel:
    runs-on: ubuntu-22.04
    container: ubuntu:22.04

    steps:
      - name: Free disk space
        run: |
            df -h
            rm -rf /usr/share/dotnet
            rm -rf /opt/ghc
            rm -rf /usr/local/lib/android
            rm -rf /usr/local/.ghcup
            apt-get clean
            df -h
          
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: kernelsu-integration
          path: kernel-source

      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y \
            bc bison build-essential ccache curl flex g++-multilib gcc-multilib \
            git gnupg gperf imagemagick lib32ncurses5-dev lib32readline-dev \
            lib32z1-dev libelf-dev liblz4-tool libncurses5 libncurses5-dev \
            libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush \
            rsync schedtool squashfs-tools xsltproc zip zlib1g-dev \
            python3 python3-pip python-is-python3 wget tar clang
          pip3 install pycrypto

      - name: Clone KernelSU Next
        run: |
          cd kernel-source
          git clone https://github.com/KernelSU-Next/KernelSU-Next.git --depth=1 -b stable KernelSU

      - name: Setup Google Toolchains (GCC 4.9)
        run: |
          mkdir -p /opt/prebuilts
          cd /opt/prebuilts
      
          # Clone ARM64 toolchain
          git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 --depth=1
      
          # Clone ARM32 toolchain
          git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 --depth=1
      
          # Find the actual bin directories
          ARM64_BIN_DIR=$(find /opt/prebuilts/aarch64-linux-android-4.9 -name "*gcc" -type f | head -1 | xargs dirname)
          ARM32_BIN_DIR=$(find /opt/prebuilts/arm-linux-androideabi-4.9 -name "*gcc" -type f | head -1 | xargs dirname)
          
          echo "ARM64_BIN_DIR=$ARM64_BIN_DIR" >> $GITHUB_ENV
          echo "ARM32_BIN_DIR=$ARM32_BIN_DIR" >> $GITHUB_ENV
          
          echo "=== Found directories ==="
          echo "ARM64: $ARM64_BIN_DIR"
          echo "ARM32: $ARM32_BIN_DIR"
          ls -la $ARM64_BIN_DIR/ | head -10
            
      - name: Setup AOSP Clang
        run: |
          mkdir -p /opt/clang-prebuilts
          cd /opt/clang-prebuilts
      
          git clone --depth=1 --filter=blob:none --sparse https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 .

          git sparse-checkout set clang-r510928

          echo "CLANG_PATH=/opt/clang-prebuilts/clang-r510928/bin" >> $GITHUB_ENV
      
      - name: Verify all toolchains
        run: |
          echo "=== Current PATH ==="
          echo $PATH
          echo ""
          echo "=== Environment Variables ==="
          echo "GCC_ARM64_PATH: $GCC_ARM64_PATH"
          echo "GCC_ARM32_PATH: $GCC_ARM32_PATH"
          echo "CLANG_PATH: $CLANG_PATH"
          echo ""
          echo "=== Checking toolchain executables ==="
          ls -la $GCC_ARM64_PATH/aarch64-linux-android-gcc || echo "ARM64 GCC not found at $GCC_ARM64_PATH"
          ls -la $GCC_ARM32_PATH/arm-linux-androideabi-gcc || echo "ARM32 GCC not found at $GCC_ARM32_PATH"
          ls -la $CLANG_PATH/clang || echo "Clang not found at $CLANG_PATH"
          
      - name: Build Kernel (Rosy 4.9) - ARM64
        run: |
          cd kernel-source
          
          # Debug: Show what's in the toolchain directories
          echo "=== Debug: Toolchain directories ==="
          find /opt/prebuilts -type f -name "*gcc*" | head -20
          echo ""
          
          # Find toolchains dynamically
          ARM64_GCC=$(find /opt/prebuilts -name "aarch64-linux-android-gcc" -type f | head -1)
          ARM32_GCC=$(find /opt/prebuilts -name "arm-linux-androideabi-gcc" -type f | head -1)
          AOSP_CLANG=$(find /opt/clang-prebuilts -name "clang" -type f | head -1)
          
          echo "Found:"
          echo "ARM64 GCC: $ARM64_GCC"
          echo "ARM32 GCC: $ARM32_GCC"
          echo "AOSP Clang: $AOSP_CLANG"
          
          if [ -z "$ARM64_GCC" ]; then
            echo "ERROR: ARM64 GCC not found!"
            exit 1
          fi
          
          if [ -z "$AOSP_CLANG" ]; then
            echo "ERROR: AOSP Clang not found!"
            exit 1
          fi
          
          # Get directory paths
          ARM64_DIR=$(dirname "$ARM64_GCC")
          ARM32_DIR=$(dirname "$ARM32_GCC")
          CLANG_DIR=$(dirname "$AOSP_CLANG")
          
          # Set up PATH
          export PATH=$CLANG_DIR:$ARM64_DIR:$ARM32_DIR:$PATH
          
          echo "=== Final PATH ==="
          echo $PATH
          echo ""
          echo "=== Checking executables ==="
          which clang || echo "Clang not found"
          which aarch64-linux-android-gcc || echo "ARM64 GCC not found"
          which arm-linux-androideabi-gcc || echo "ARM32 GCC not found"
          
          # Set environment variables
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-android-
          export CROSS_COMPILE_ARM32=arm-linux-androideabi-
          export CC=clang
          
          # Build kernel
          make msm8937-perf_defconfig
          make olddefconfig
          make -j$(nproc --all) Image.gz-dtb

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ksu-next-build-${{ github.run_number }}
          path: |
            kernel-source/arch/arm64/boot/Image.gz-dtb
            kernel-source/KernelSU/kernel/kernel_su.ko
